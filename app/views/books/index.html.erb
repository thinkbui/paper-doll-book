<h1>Books</h1>
<a href="<%= new_book_path() %>">New Book</a>
<br />
<br />
<table>
	<thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td>
          <a href="<%= book_path(book) %>">
            <%= book.name %>
          </a>
        </td>
        <td><%= book.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>